⚠️ STOP — Read these 3 rules before every action:
1. One shared AI_OS and one shared docs folder live at the Master Collection parent root.
2. The website sells and serves packages; the Webflow app installs inside the buyer's Webflow project.
3. Use the bare Flow-Goodies/shadcn light-dark style unless Maria explicitly changes the design direction.

# Master Collection — CLAUDE.md

## Quick Start

Read:

1. `AI.md`
2. `AGENTS.md`
3. `docs/DOCS_INDEX.md`
4. The relevant child guidance:
   - `app/CLAUDE.md`
   - `site/CLAUDE.md`

## AI_OS Reference

This project uses the AI Operating System at:

```text
C:\Users\maria\Desktop\pessoal\FLOW_PARTY\MASTER-COLLECTION\AI_OS
```

Do not create child AI_OS folders. Do not modify AI_OS core files unless Maria asks for an AI_OS sync or setup change.

Key files:

- `AI_OS/AI_OPERATING_SYSTEM.md`
- `AI_OS/CONTEXT_RULES.md`
- `AI_OS/AI_DECISION_LOG.md`
- `AI_OS/SESSION-PROMPTS/AI_SESSION_START.md`

## Domain Glossary

| User Term | Project Meaning | Notes |
|-----------|-----------------|-------|
| Master Collection | Whole product ecosystem | Parent of both app and site |
| app | Webflow Designer Extension | Lives in `app/`; installs packages inside Webflow |
| site | Public/account website | Lives in `site/`; sells and serves package access |
| package | Prepared install payload | Eventually `MasterCollectionPackage` / `master-collection-package@1` |
| install code | Account-issued code used by the Webflow app | The site issues it; the app redeems it |
| minimal converter | Old vehicle/playground concept | Replaced by the Master Collection website/platform |

Ask before mapping new business terms to database fields once database work begins.

## Build & Verification Commands

No runtime has been scaffolded yet.

When the child projects are created, add their commands here and in `AGENTS.md`.

## Error Recovery

If the same approach fails twice, stop and report options. Do not try a third variation without direction.

## Compaction Instructions

When context is compacted, preserve:

1. The current workstream: parent docs, `site/`, or `app/`.
2. Files modified this session.
3. Active plan path from `docs/plans/`.
4. Decisions about site/app boundaries.
5. Any missing credentials or blocked setup steps.

⚠️ FINAL CHECK — These rules are non-negotiable:
1. One shared AI_OS and one shared docs folder live at the Master Collection parent root.
2. The website sells and serves packages; the Webflow app installs inside the buyer's Webflow project.
3. Use the bare Flow-Goodies/shadcn light-dark style unless Maria explicitly changes the design direction.

